# Ensure cross-platform compatibility
target_platform=linux
target_arch=x64
target_libc=glibc

# Use the platform-specific SWC binary for the current platform
# This will automatically select the correct SWC binary for the deployment environment
